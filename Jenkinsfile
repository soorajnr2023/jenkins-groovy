pipeline{
agent any
stages{
 stage('Printing a message '){
steps{(sh 'echo Job is building' )
       (sh 'mvn --version')
      ( sh 'echo package is deploying')
      (sh 'echo docker is deployed...successfully')
      
}
 }
}
}
